version: "1.0"
description: "Permission policy for AVS (Azure VMware Solution) listing example"
permissions:
  network:
    allow:
      - host: "management.azure.com"
  environment:
    allow:
      - key: AZURE_SUBSCRIPTION
      - key: AZURE_TOKEN
